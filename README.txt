# Рассылка писем

**Описание**:

Функция send_email позволяет отправлять электронные письма. Она принимает 2 обычных аргумента: сообщение и получатель, а также один обязательно именованный аргумент со значением по умолчанию — отправитель.

**Как использовать**:

1. Создайте файл с функцией send_email.
2. Импортируйте необходимые библиотеки.
3. Реализуйте логику функции в соответствии с описанием.
4. Протестируйте функцию.
5. Отправьте проект на GitHub.
6. Поделитесь ссылкой на проект с другими разработчиками.
7. При необходимости предоставьте дополнительную документацию или примеры использования.

## Пример использования
```python
from email import message

def send_email(message, recipient, sender='default@example.com'):
    # Реализация логики функции
    pass

send_email('Привет!', 'user@example.ru')
```

**Обязательные аргументы**:
* **message** — сообщение, которое будет отправлено.
* **recipient** — адрес получателя.

**Именованный аргумент**:
* **sender** — адрес отправителя (по умолчанию «default@example.com»).

**Логика функции**:
1. Проверка на корректность e-mail отправителя и получателя.
2. Проверка на отправку самому себе.
3. Проверка на отправителя по умолчанию.

Обратите внимание, что это всего лишь пример файла README для проекта, который реализует функцию отправки электронных писем. Вы можете адаптировать его под свои нужды.
